#!/usr/bin/env node

var mapshaper = require("../").enableLogging(),
    commands = mapshaper.parseCommands(process.argv.slice(2));

mapshaper.runCommands(commands, done);

function done(err) {
  if (err) mapshaper.stop(err);
  process.exit(0);
}
