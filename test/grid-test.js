var api = require('../'),
    assert = require('assert');

describe('mapshaper-polygon-grid.js', function () {

  it('-grid fails if target has projected coordinates', function (done) {
    var cmd = '';
    done();
  })
});
