import {
  appendNewPath,
  deleteLastPath,
  getLastArcCoords
} from './gui-drawing-utils';

export function snipLineAtVertex(lyr, fid, vid) {
  // find the feature part and arc containing the vertex
  // divide the one feature into two features
  // divide the arc into two new arcs
  var arc1, arc2;
  // divide the feature into two parts
}

export function mergeLinesAtVertex(lyr) {

}
