/* @requires mapshaper-gui-lib */

function HighlightRectangle(el) {
  var box =  El('div').addClass('zoom-box').appendTo(el),
      boxStroke = 2;
  this.show = function(x, y, w, h) {

  };
  this.hide = function() {};
}
