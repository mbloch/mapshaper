
var ImageUtils = {
  index: {},
  preload: function(url) {
    if (! url in this.index) {
      var img = new Image();
      img.src = url;
      this.index[url] = img;
    }
  },
  getImage: function(url) {
    return this.index[url] || null;
  }
};