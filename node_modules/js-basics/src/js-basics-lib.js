/* 
@requires
arrayutils
data
dateutils
events
format
median
nodejs
*/

var api = {
  Utils: Utils,
  Opts: Opts,
  Env: Env,
  trace: trace,
  error: error
};

if (Node.inNode) {
  module.exports = api;
} else {
  Opts.extendNamespace("nytg", api);
}
