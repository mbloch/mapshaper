/* core, nodejs */

var api = {
  Utils: Utils,
  Opts: Opts,
  Env: Env,
  trace: trace,
  assert: assert,
  error: error
};

if (Node.inNode) {
  module.exports = api;
} else {
  Opts.extendNamespace("nytg", api);
}